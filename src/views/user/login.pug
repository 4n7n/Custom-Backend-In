html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Reservas Login
        link(rel="stylesheet" href="/css/layout.css")
        link(rel="stylesheet" href="/css/loginStyles.css")

    body
        main
            div.title
                h1 Proyecto Reservas
            div.login-box
                h2 Iniciar Sesión
                form.login-form(action="/login" method="POST")
                    label(for="email") Correo Electrónico
                    input#email(type="email" placeholder="usuarioejemplo@correo.com" name="email" required)

                    label(for="password") Contraseña
                    input#password(type="password" placeholder="Contraseña" name="password" required)

                    if(message && messageType)
                        p(style=`color: red` class=`message ${messageType}`)= message  
                    button.login-button(type="submit") Entrar

                    a(href='/register')    
                        button.azure-button(type="button") Registro